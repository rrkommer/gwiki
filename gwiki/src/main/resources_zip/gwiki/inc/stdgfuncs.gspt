<%
public String esc(String text)
{
	return de.micromata.genome.gwiki.utils.WebUtils.escapeHtml(text); 
}
public String esc(Object text)
{
	if (text == null)
		return "";
	return de.micromata.genome.gwiki.utils.WebUtils.escapeHtml(text.toString()); 
}

%>