{pageintro}
With Macro {{if}} conditional output can be created.
{pageintro}

The macro has following test attributes:
- lang: render block if language is set.
- right: render block if current user has right.
- renderMode: render block if render mode is set.

{todo}
Introduce alias to macro with name {{ifnot}}.
{todo}

Java class: {{de.micromata.genome.gwiki.page.impl.wiki.macros.GWikiIfMacro}}.
